<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.module.system.biz.dal.mapper.SysUserMapper">

    <resultMap type="com.admin.module.system.biz.dal.dataobject.SysUserDO" id="SysUserResult">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="nickname" column="nickname"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="gender" column="gender"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="deleted" column="deleted"/>
        <result property="loginIp" column="login_ip"/>
        <result property="loginDate" column="login_date"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
        <result property="version" column="version"/>
    </resultMap>

    <sql id="selectUserVo">
        select id, username, nickname, password, email, phone, gender, avatar, status, del_flag, login_ip, login_date, 
               create_by, create_time, update_by, update_time, remark, version
        from sys_user
    </sql>

    <select id="selectUserByUsername" parameterType="String" resultMap="SysUserResult">
        <include refid="selectUserVo"/>
        where username = #{username} and del_flag = 0
    </select>

    <select id="selectUserList" resultMap="SysUserResult">
        <include refid="selectUserVo"/>
        where del_flag = 0
        <if test="username != null and username != ''">
            AND username like concat('%', #{username}, '%')
        </if>
        <if test="nickname != null and nickname != ''">
            AND nickname like concat('%', #{nickname}, '%')
        </if>
        <if test="phone != null and phone != ''">
            AND phone like concat('%', #{phone}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="beginTime != null and beginTime != ''"><!-- 开始时间检索 -->
            AND date_format(create_time,'%y%m%d') &gt;= date_format(#{beginTime},'%y%m%d')
        </if>
        <if test="endTime != null and endTime != ''"><!-- 结束时间检索 -->
            AND date_format(create_time,'%y%m%d') &lt;= date_format(#{endTime},'%y%m%d')
        </if>
        order by create_time desc
    </select>

    <select id="checkUsernameUnique" resultType="int">
        select count(1) from sys_user 
        where username = #{username} 
        <if test="id != null">
            and id != #{id}
        </if>
        and del_flag = 0
        limit 1
    </select>

    <select id="checkPhoneUnique" resultType="int">
        select count(1) from sys_user 
        where phone = #{phone} 
        <if test="id != null">
            and id != #{id}
        </if>
        and del_flag = 0
        limit 1
    </select>

    <select id="checkEmailUnique" resultType="int">
        select count(1) from sys_user 
        where email = #{email} 
        <if test="id != null">
            and id != #{id}
        </if>
        and del_flag = 0
        limit 1
    </select>

</mapper>